<% controllers = ['settings', 'dashboard'] %>
<% dropdown_class = (controllers & controller_path.split('/')).empty? ? '' : 'active' %>
<li class="nav-item dropdown account-settings text-left <%= dropdown_class %>">
  <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
    <span><%= current_account.email %></span>
    <b class="caret"></b>
  </a>
  <div class="dropdown-menu">
    <a class="dropdown-item" href="<%= ENV['BARONG_DOMAIN']%>">
      <i class="fa fa-user"></i><span><%= t('header.my_profile') %></span>
    </a>
    <% if current_account.role.admin? %>
      <a class="dropdown-item" href="<%= ENV['API_CORS_ORIGINS']+'/admin' %>">
        <i class="fa fa-tachometer-alt"></i>
        <span><%= t('header.admin') %></span>
      </a>
    <% end %>
    <a class="dropdown-item" href="<%= destroy_account_session_path %>">
      <i class="fa fa-sign-out-alt"></i>
      <span><%= t('header.signout') %></span>
    </a>
  </div>
</li>
